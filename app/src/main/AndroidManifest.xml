<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ch.epfl.sweng.erpa">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ErpaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ERPAStyle"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.CreateGameActivity" />
        <activity android:name=".activities.DiceActivity" />
        <activity android:name=".activities.DiceAnimationActivity" />
        <activity android:name=".activities.ExampleForToolbarActivity" />
        <activity android:name=".activities.JoinGameActivity" />
        <activity android:name=".activities.GameListActivity" android:launchMode="singleTop" />
        <activity android:name=".activities.GameViewerActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MyAccountActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.RemoteServiceGreeterActivity" />
        <activity android:name=".activities.SelectRemoteServicesProviderActivity" />
        <activity android:name=".activities.SignupActivity" />
        <activity android:name=".activities.SortActivity" />
        <activity android:name=".activities.UserProfileActivity" />
    </application>
</manifest>
